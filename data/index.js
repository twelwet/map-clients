'use strict';

const nodes = require(`./json/nodes.json`);
const objects = require(`./json/objects.json`);
const fiberLinesBackbone = require(`./json/fiber-lines-backbone.json`);
const fiberLinesCityNet = require(`./json/fiber-lines-city-net.json`);

module.exports = {nodes, objects, fiberLinesBackbone, fiberLinesCityNet};
